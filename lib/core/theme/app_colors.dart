import 'package:flutter/material.dart';

/// Único lugar del proyecto donde deberían escribirse valores `Color(0x...)`.
/// El resto de la UI consume estos colores a través de `Theme.of(context)`.
abstract final class AppColors {
  static const univalleRed = Color(0xFFFF0000);
}
